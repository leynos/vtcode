# vtcode-agent-plugins

Agent Plugins loader for VT Code. Portable plugin format: a directory with a
root `plugin.json` manifest, optional `skills/*/SKILL.md` Agent Skills, and
optional `mcp.json` MCP server configuration.
